SHADERS:

To draw a cube we need to specify its vertices
To define these attributes, we place the data in an array. In here we are going to consider an array for each attribute

float position[] = {-1.0, 0,0, 5.0, 1.0,
					1.0, 0,0, 5.0, 1.0,
					0.0, 2.0, 5.0, 1.0};

float textureCoord[] = { ... };
float normal[] = { ... };